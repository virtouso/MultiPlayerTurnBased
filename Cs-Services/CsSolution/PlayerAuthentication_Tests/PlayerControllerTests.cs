using NUnit.Framework;
using SharedModels;
using System.Collections.Generic;



namespace PlayerAuthentication_Tests
{

    internal class PlayerControllerTests
    {
        [SetUp]
        public void SetUp()
        {

        }


        [Test]
        public void GetNewPlayer()
        {

            //var identity = new Player.Identity();
            //identity._id = "dd";
            //identity.UniqueName = "ddd";

            //var progress = new Player.Progress();
            //progress.Gold = 100;
            //progress.Silver = 100;
            //var tracking = new Player.Tracking(new List<Player.Tracking.Device>());
            //Player player = new Player(identity, tracking, progress);




            //Assert.AreEqual(player.PlayerTracking.Devices.Count, 0);
            //Assert.AreEqual(player.PlayerIdentity._id, "dd");
            //Assert.AreEqual(player.PlayerIdentity.UniqueName, "ddd");
            //Assert.AreEqual(player.PlayerProgress.Gold, 100);
            //Assert.AreEqual(player.PlayerProgress.Silver, 100);

        }


    }
}
